## install zoxide

# first install fzf
`sudo pacman -S fzf`

# then install rust
`sudo pacman -S rust`

# lastly, install zoxide it's self
`sudo pacman -S zoxide`

## install ncpamixer
`paru -S ncpamixer`
